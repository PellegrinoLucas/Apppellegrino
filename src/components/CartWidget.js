const CartWidget=() => {
    const cantidad = 1
    return(
        <div>
        <p>{cantidad}</p>
        <span className="material-icons">shopping_cart</span>
        </div>
    )
};

export default CartWidget;